#!/usr/bin/env php
<?php

namespace Mgrunder\Fuzzer;

require_once __DIR__ . '/' . '../vendor/autoload.php';

use Mgrunder\Fuzzer\Cmd\get;

$cfg = new FuzzConfig('localhost', 8080, 10, 10);
$obj = new get($cfg);
print_r($obj->fuzz());
